@model IEnumerable<EnterprisePortal.Models.QuickLinksCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LinkCatName)
        </th>
        @*<th></th>*@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LinkCatName)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.LinkCatId }) |
                    @Html.ActionLink("Details", "Details", new { id=item.LinkCatId }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.LinkCatId })
                </td>*@
        </tr>
    }
</table>